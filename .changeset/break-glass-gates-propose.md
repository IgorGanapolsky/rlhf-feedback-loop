---
"thumbgate": patch
---

Add a propose-only break-glass `--gates` doctor for issue #3702 leftover: emit a unified diff that sets named gates to warn, never write `config/gates/default.json`, never live-apply, never pack.
