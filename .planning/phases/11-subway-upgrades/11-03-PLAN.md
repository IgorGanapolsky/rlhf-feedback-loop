---
phase: 11-subway-upgrades
plan: 03
type: auto
autonomous: true
wave: 2
depends_on: [11-01]
requirements: [SUBW-02, SUBW-05]
---

# Plan 11-03: DPO Optimizer Port to Subway

## Objective

Port `scripts/dpo-optimizer.js` and its dependency `scripts/export-dpo-pairs.js` to Subway's `.claude/scripts/feedback/` with 3-level PATH surgery and a Jest test suite covering `dpoLogRatio`, `buildPreferencePairs`, `applyDpoAdjustments`, and `run`.

## Context

- Source: `/Users/ganapolsky_i/workspace/git/igor/rlhf/scripts/dpo-optimizer.js`
- Depends on `export-dpo-pairs.js` and `thompson-sampling.js` (11-01 provides thompson-sampling.js)
- DPO optimizer reads `memory-log.jsonl` from `RLHF_FEEDBACK_DIR`
- Writes `dpo-model.json` to feedbackDir
- Tests: use tmp directories and create minimal fixture `memory-log.jsonl` data

## Tasks

### Task 1: Port export-dpo-pairs.js and dpo-optimizer.js to Subway
**type**: auto

**Step 1a: Read source export-dpo-pairs.js**
Read `/Users/ganapolsky_i/workspace/git/igor/rlhf/scripts/export-dpo-pairs.js` to understand its exports.

**Step 1b: Port export-dpo-pairs.js**
Create `/Users/ganapolsky_i/workspace/git/Subway_RN_Demo/.claude/scripts/feedback/export-dpo-pairs.js`.

Changes:
- Keep all exports: `buildDpoPairs`, `readJSONL`, and any other exports from source
- No path surgery needed (uses passed-in paths, not __dirname-based defaults)

**Step 1c: Port dpo-optimizer.js**
Create `/Users/ganapolsky_i/workspace/git/Subway_RN_Demo/.claude/scripts/feedback/dpo-optimizer.js`.

Changes from rlhf source:
1. `require('./export-dpo-pairs')` → `require('./export-dpo-pairs')` (same dir, no change)
2. `require('./thompson-sampling')` → `require('./thompson-sampling')` (same dir, no change)
3. Default feedbackDir uses `os.homedir()` path — no Subway-specific path change needed
4. Default modelPath in `run()`: change from `path.join(process.cwd(), '.claude', ...)` to use `os.homedir()` or keep cwd-based

**Done criteria**: Both files exist; `node -e "require('/Users/ganapolsky_i/workspace/git/Subway_RN_Demo/.claude/scripts/feedback/dpo-optimizer.js')"` exits 0.

### Task 2: Write Jest test suite for DPO optimizer
**type**: auto

Create `/Users/ganapolsky_i/workspace/git/Subway_RN_Demo/scripts/__tests__/dpo-optimizer.test.js` with 8 tests:

1. `dpoLogRatio(1.0, 0.01)` returns positive value (chosen >> rejected)
2. `dpoLogRatio(0.01, 1.0)` returns negative value (rejected >> chosen)
3. `dpoLogRatio(1.0, 1.0)` returns 0 (balanced)
4. `dpoLogRatio` result is in range [-1, +1]
5. `buildPreferencePairs` with empty memory-log.jsonl returns empty object `{}`
6. `applyDpoAdjustments` with empty pairs does not mutate model (0 changes)
7. `run()` with empty feedbackDir creates `dpo-model.json` with `pairs_processed: 0`
8. `run()` result has `adjustments` and `pairs_processed` keys

Use `beforeEach(() => { jest.resetModules(); })`.
Create tmp dirs with `fs.mkdtempSync(path.join(os.tmpdir(), 'dpo-test-'))` for file I/O tests.
Create minimal `memory-log.jsonl` fixture (empty or 2 entries) for integration tests.

Run: `npx jest --config jest.governance.config.js scripts/__tests__/dpo-optimizer.test.js --no-coverage` from Subway root.

**Done criteria**: All 8 tests pass, 0 failures.

## Verification

```bash
cd /Users/ganapolsky_i/workspace/git/Subway_RN_Demo && npx jest --config jest.governance.config.js scripts/__tests__/dpo-optimizer.test.js --no-coverage 2>&1 | tail -10
```

## Success Criteria

- `export-dpo-pairs.js` and `dpo-optimizer.js` exist in Subway `.claude/scripts/feedback/`
- All 8 Jest tests pass with 0 failures
- SUBW-02 satisfied (DPO optimizer available in Subway)
