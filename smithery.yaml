startCommand:
  type: "stdio"
  configSchema:
    type: "object"
    properties:
      mcpProfile:
        type: "string"
        description: "MCP profile to use (default, readonly, locked)"
        default: "default"
    required: []
  commandFunction:
    command: "npx"
    args:
      - "-y"
      - "mcp-memory-gateway"
      - "serve"
description: "Agent quality feedback loop with Pre-Action Gates — configurable checkpoints that block dangerous tool calls before they execute. Includes prevention rules, RLHF capture, and commerce quality scores."
