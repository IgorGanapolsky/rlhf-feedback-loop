{
  "name": "mcp-memory-gateway-worker",
  "version": "1.0.0",
  "description": "Cloudflare Workers MCP server for mcp-memory-gateway Pro tier ($29/mo)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "tsc -p tsconfig.json --noEmit",
    "types": "wrangler types"
  },
  "dependencies": {
    "stripe": "^20.4.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260313.1",
    "typescript": "^5.5.0"
  }
}
